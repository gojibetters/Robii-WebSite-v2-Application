<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>

  <% cssDirectoriesRender.forEach((urlArchive)=> {%>
    <link rel="stylesheet" type="text/css" href="<%= urlArchive.replace('pages/login/','') %>" />%> <% }) %>
</head>

<body>
  <% htmlDirectoriesRender.forEach((urlArchive)=> {%>
    <%- include( urlArchive.replace('pages/login/', '' )) %>
      <% }) %>
        <% javascriptDirectoriesRender.forEach((urlArchive)=> {%>
          <script src="<%= urlArchive.replace('pages/login/','')  %>"></script>
          <% }) %>
</body>

</html>
